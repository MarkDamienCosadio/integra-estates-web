# Sanity Studio (used by sanity/sanity.config.ts)
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Frontend (Vite) — used in src code when integrating client queries
VITE_SANITY_PROJECT_ID=
VITE_SANITY_DATASET=production
VITE_SANITY_API_VERSION=2023-10-01
# Optional: for draft preview only (never expose in client builds)
VITE_SANITY_READ_TOKEN=